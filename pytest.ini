#pytest.ini

[pytest]
testpaths=src/autostory/tests/
addopts=
    --maxfail=1
    --cov-config=pytest_cov.ini
    --cov=src/autostory/
    --cov-report=term-missing
    --cov-branch
    --no-cov-on-fail
minversion = 5.0
